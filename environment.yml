name: pyshockflow
channels:
  - conda-forge

dependencies:
  - python=3.12
  - numpy
  - scipy
  - matplotlib
  - ipython
  - ipykernel
  - jupyter_client
  - jupyter_core
  - pip

  - pip:
      - coolprop
      - pytest